---
phase: 08-state-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/types.ts
  - src/server/parser.ts
  - src/server/state-machine.ts
autonomous: true
requirements: [DETECT-01, DETECT-05]

must_haves:
  truths:
    - "turn_duration JSONL 레코드가 파싱되어 AgentEvent로 변환된다"
    - "turn_duration 수신 시 에이전트가 즉시 waiting 상태가 되고 모든 타이머가 취소된다"
    - "텍스트 전용 턴에서 5초간 새 데이터가 없으면 에이전트가 waiting으로 전환된다"
    - "permissionPending 필드가 AgentState에 존재하고 WebSocket으로 전달된다"
  artifacts:
    - path: "src/shared/types.ts"
      provides: "AgentState.permissionPending boolean field"
      contains: "permissionPending"
    - path: "src/server/parser.ts"
      provides: "turn_duration and queue-operation parsing"
      contains: "turn_duration"
    - path: "src/server/state-machine.ts"
      provides: "Per-agent timer infrastructure, turn_duration handler, text-idle timer"
      contains: "TEXT_IDLE_DELAY_MS"
  key_links:
    - from: "src/server/parser.ts"
      to: "src/server/state-machine.ts"
      via: "AgentEvent with type turn_end or assistant_text"
      pattern: "turn_end"
    - from: "src/server/state-machine.ts"
      to: "WebSocket broadcast"
      via: "agent-update emit with permissionPending in AgentState"
      pattern: "permissionPending"
---

<objective>
Turn_duration 파서와 텍스트 턴 waiting 타이머를 구현한다.

Purpose: JSONL 스트림에서 확정적 턴 종료 신호(turn_duration)를 감지하고, 텍스트 전용 턴에서 5초 미활동 시 waiting으로 전환하는 기반을 만든다. AgentState에 permissionPending 필드를 추가하여 Plan 02의 permission 타이머가 바로 사용할 수 있도록 준비한다.
Output: 확장된 types.ts, parser.ts, state-machine.ts (per-agent 타이머 인프라 포함)
</objective>

<execution_context>
@C:/Users/ldm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ldm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/shared/types.ts:
```typescript
export type AgentStatus = 'idle' | 'typing' | 'reading' | 'executing' | 'waiting' | 'done' | 'error'
  | 'walk_down' | 'walk_up' | 'walk_right' | 'walk_left';

export interface AgentEvent {
  timestamp: number;
  sessionId: string;
  type: string;
  content: string;
  raw: unknown;
}

export interface AgentState {
  id: string;
  name: string;
  sessionId: string;
  status: AgentStatus;
  lastAction: string;
  lastUpdated: number;
  position: { x: number; y: number };
}

export interface WSMessage {
  type: 'agent-update' | 'agent-added' | 'agent-removed';
  payload: AgentState | AgentState[];
}
```

From src/server/parser.ts (line 54-56):
```typescript
// Skip file-history-snapshot and system metadata
if (entryType === 'file-history-snapshot' || entryType === 'system') {
  return null;
}
```

From src/server/state-machine.ts:
```typescript
// Timeout thresholds (ms)
const WAITING_TIMEOUT = 30_000;
const IDLE_TIMEOUT = 60_000;
const DONE_TIMEOUT = 5 * 60_000;

// processEvent() at line 83-101
// deriveStatus() at line 145-188
// checkTimeouts() at line 262-284
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AgentState 타입 확장 + parser turn_duration/queue-operation 파싱</name>
  <files>src/shared/types.ts, src/server/parser.ts</files>
  <read_first>src/shared/types.ts, src/server/parser.ts</read_first>
  <action>
**1. src/shared/types.ts — AgentState에 permissionPending 추가:**

AgentState 인터페이스에 다음 필드를 추가한다:
```typescript
export interface AgentState {
  // ... existing fields ...
  permissionPending: boolean;   // true when waiting for tool permission (7s timeout)
}
```

AgentEvent 인터페이스에 toolName optional 필드를 추가한다 (permission 타이머용):
```typescript
export interface AgentEvent {
  // ... existing fields ...
  toolName?: string;             // extracted tool name for tool_use events
}
```

**2. src/server/parser.ts — system 타입에서 turn_duration 파싱:**

현재 line 54-56에서 `entryType === 'system'` 을 무조건 skip한다. 이를 수정:

- `system` 타입 중 `subtype: 'turn_duration'`인 레코드만 파싱하고, 나머지 system은 여전히 skip
- RawJSONLEntry에 `subtype?: string` 필드 추가
- turn_duration 파싱 시 AgentEvent.type을 `'turn_end'`로 설정

수정 코드:
```typescript
// Replace the existing system skip at line 54-56 with:
if (entryType === 'file-history-snapshot') {
  return null;
}

if (entryType === 'system') {
  // Parse turn_duration subtype — definitive turn end signal
  if (raw.subtype === 'turn_duration') {
    return {
      timestamp,
      sessionId,
      type: 'turn_end',
      content: 'turn_duration',
      raw,
    };
  }
  return null;
}
```

RawJSONLEntry 인터페이스에 추가:
```typescript
subtype?: string;  // e.g. 'turn_duration' for system type
```

**3. src/server/parser.ts — progress 타입에서 queue-operation 파싱:**

tool_result 처리 뒤에 progress 타입 파싱을 추가한다:
```typescript
if (entryType === 'progress') {
  // queue-operation indicates background agent activity
  if (raw.subtype === 'queue-operation') {
    return {
      timestamp,
      sessionId,
      type: 'queue-operation',
      content: summarizeContent(raw.content),
      raw,
    };
  }
  return null;
}
```

**4. src/server/parser.ts — tool_use 파싱에 toolName 추가:**

parseAssistantEntry의 tool_use 블록에서 toolName을 AgentEvent에 포함:
```typescript
if (block.type === 'tool_use' && block.name) {
  return {
    timestamp,
    sessionId,
    type: `tool_use:${block.name}`,
    content: summarizeToolUse(block.name, block.input),
    toolName: block.name,
    raw,
  };
}
```

**5. src/server/parser.ts — run_in_background 감지:**

parseAssistantEntry의 tool_use 블록에서 `run_in_background` 플래그가 있는 Bash 도구를 감지:
```typescript
if (block.type === 'tool_use' && block.name) {
  const isBackground = block.name === 'Bash' &&
    block.input?.run_in_background === true;
  return {
    timestamp,
    sessionId,
    type: isBackground ? 'background-tool' : `tool_use:${block.name}`,
    content: summarizeToolUse(block.name, block.input),
    toolName: block.name,
    raw,
  };
}
```
  </action>
  <verify>
    <automated>cd D:/work/dev/github/ldmprog/iosys-pixel-lab && npx tsc --noEmit 2>&1</automated>
  </verify>
  <acceptance_criteria>
- `grep -n "permissionPending" src/shared/types.ts` shows `permissionPending: boolean` in AgentState
- `grep -n "toolName" src/shared/types.ts` shows `toolName?: string` in AgentEvent
- `grep -n "turn_end" src/server/parser.ts` shows turn_duration -> turn_end parsing
- `grep -n "turn_duration" src/server/parser.ts` shows subtype check for turn_duration
- `grep -n "queue-operation" src/server/parser.ts` shows progress type parsing
- `grep -n "background-tool" src/server/parser.ts` shows run_in_background detection
- `grep -n "subtype" src/server/parser.ts` shows subtype field in RawJSONLEntry
- `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>AgentState has permissionPending boolean. Parser handles turn_duration (type='turn_end'), queue-operation, and background-tool events. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Per-agent 타이머 인프라 + turn_end 핸들러 + text-idle 타이머</name>
  <files>src/server/state-machine.ts</files>
  <read_first>src/server/state-machine.ts, src/shared/types.ts, src/server/parser.ts</read_first>
  <action>
**1. 상수 추가** (파일 상단, 기존 타임아웃 상수 아래):
```typescript
// New detection timers (ms)
const PERMISSION_TIMER_DELAY_MS = 7_000;   // 7s after non-exempt tool_use -> permission
const TEXT_IDLE_DELAY_MS = 5_000;           // 5s text-only idle -> waiting

// Tools exempt from permission timer
const PERMISSION_EXEMPT_TOOLS = new Set(['Task', 'Agent', 'AskUserQuestion']);
```

**2. Per-agent 타이머 타입** (클래스 위에 선언):
```typescript
interface AgentTimers {
  permissionTimer: ReturnType<typeof setTimeout> | null;
  textIdleTimer: ReturnType<typeof setTimeout> | null;
  hadToolsInTurn: boolean;
}
```

**3. AgentStateMachine 클래스에 타이머 맵 추가:**
```typescript
private agentTimers: Map<string, AgentTimers> = new Map();
```

**4. 타이머 헬퍼 메서드 추가:**
```typescript
/** Get or create timers for an agent */
private getTimers(agentId: string): AgentTimers {
  let timers = this.agentTimers.get(agentId);
  if (!timers) {
    timers = { permissionTimer: null, textIdleTimer: null, hadToolsInTurn: false };
    this.agentTimers.set(agentId, timers);
  }
  return timers;
}

/** Cancel all active timers for an agent */
private cancelAllTimers(agentId: string): void {
  const timers = this.agentTimers.get(agentId);
  if (!timers) return;
  if (timers.permissionTimer) { clearTimeout(timers.permissionTimer); timers.permissionTimer = null; }
  if (timers.textIdleTimer) { clearTimeout(timers.textIdleTimer); timers.textIdleTimer = null; }
}

/** Cancel permission timer only */
private cancelPermissionTimer(agentId: string): void {
  const timers = this.agentTimers.get(agentId);
  if (!timers?.permissionTimer) return;
  clearTimeout(timers.permissionTimer);
  timers.permissionTimer = null;
}

/** Cancel text-idle timer only */
private cancelTextIdleTimer(agentId: string): void {
  const timers = this.agentTimers.get(agentId);
  if (!timers?.textIdleTimer) return;
  clearTimeout(timers.textIdleTimer);
  timers.textIdleTimer = null;
}
```

**5. processEvent() 전면 확장** — 기존 로직을 유지하면서 새 이벤트 타입 처리 추가:

```typescript
processEvent(event: AgentEvent): void {
  let agent = this.getAgentBySessionId(event.sessionId);
  if (!agent) {
    agent = this.createAgent(event.sessionId);
  }

  const timers = this.getTimers(agent.id);

  // --- DETECT-03: Any new data cancels active timers ---
  this.cancelAllTimers(agent.id);
  if (agent.permissionPending) {
    agent.permissionPending = false;
    // Will emit update below
  }

  // --- DETECT-01: turn_end (definitive turn end) ---
  if (event.type === 'turn_end') {
    agent.status = 'waiting';
    agent.lastAction = event.content;
    agent.lastUpdated = event.timestamp;
    timers.hadToolsInTurn = false;
    this.emit('agent-update', { ...agent });
    return;
  }

  // --- DETECT-04: queue-operation (background agent) ---
  if (event.type === 'queue-operation') {
    // Track but don't change agent status — handled by Plan 02
    agent.lastUpdated = event.timestamp;
    this.emit('agent-update', { ...agent });
    return;
  }

  // --- DETECT-04: background-tool (run_in_background) ---
  if (event.type === 'background-tool') {
    agent.lastUpdated = event.timestamp;
    agent.lastAction = event.content;
    this.emit('agent-update', { ...agent });
    return;
  }

  // --- Standard event processing ---
  const previousStatus = agent.status;
  const newStatus = this.deriveStatus(event);

  agent.status = newStatus;
  agent.lastAction = event.content;
  agent.lastUpdated = event.timestamp;

  // --- DETECT-02 setup: Start permission timer for non-exempt tool_use ---
  if (event.type.startsWith('tool_use:') && event.toolName) {
    timers.hadToolsInTurn = true;
    if (!PERMISSION_EXEMPT_TOOLS.has(event.toolName)) {
      timers.permissionTimer = setTimeout(() => {
        timers.permissionTimer = null;
        const currentAgent = this.agents.get(agent!.id);
        if (currentAgent && !currentAgent.permissionPending) {
          currentAgent.permissionPending = true;
          this.emit('agent-update', { ...currentAgent });
        }
      }, PERMISSION_TIMER_DELAY_MS);
    }
  }

  // --- DETECT-05: Start text-idle timer for text-only response ---
  if (event.type === 'assistant_text') {
    timers.textIdleTimer = setTimeout(() => {
      timers.textIdleTimer = null;
      const currentAgent = this.agents.get(agent!.id);
      if (currentAgent && currentAgent.status !== 'waiting' && currentAgent.status !== 'done') {
        currentAgent.status = 'waiting';
        this.emit('agent-update', { ...currentAgent });
      }
    }, TEXT_IDLE_DELAY_MS);
  }

  if (previousStatus !== newStatus || agent.permissionPending === false) {
    this.emit('agent-update', { ...agent });
  }
}
```

**6. createAgent() 수정** — permissionPending 초기화:

createAgent에서 새 AgentState 객체에 `permissionPending: false` 추가:
```typescript
const agent: AgentState = {
  id: agentId,
  name: `Claude-${abbreviate(projectName)}-${this.agentCounter}`,
  sessionId,
  status: 'idle',
  lastAction: '',
  lastUpdated: Date.now(),
  permissionPending: false,
  position: seat
    ? { x: seat.tileX, y: seat.tileY }
    : { x: 2 + (this.agentCounter % 6) * 2, y: 2 + Math.floor(this.agentCounter / 6) * 2 },
};
```

**7. removeAgent() 수정** — 타이머 정리:

removeAgent에서 agentTimers도 정리:
```typescript
private removeAgent(agentId: string): void {
  this.cancelAllTimers(agentId);
  this.agentTimers.delete(agentId);
  // ... existing logic
}
```

**8. stop() 수정** — 모든 타이머 정리:

```typescript
stop(): void {
  if (this.timeoutCheckInterval) {
    clearInterval(this.timeoutCheckInterval);
    this.timeoutCheckInterval = null;
  }
  // Clear all per-agent timers
  for (const agentId of this.agentTimers.keys()) {
    this.cancelAllTimers(agentId);
  }
  this.agentTimers.clear();
}
```

**주의사항:**
- 기존 checkTimeouts() 5초 폴링은 그대로 유지 — 폴백 역할 (30s/60s/5m)
- 새 타이머(7s permission, 5s text-idle)는 per-agent setTimeout으로 더 정밀하게 동작
- import에서 AgentEvent의 toolName 사용하므로 types.ts 변경이 먼저 필요
  </action>
  <verify>
    <automated>cd D:/work/dev/github/ldmprog/iosys-pixel-lab && npx tsc --noEmit 2>&1</automated>
  </verify>
  <acceptance_criteria>
- `grep -n "PERMISSION_TIMER_DELAY_MS" src/server/state-machine.ts` shows `7_000`
- `grep -n "TEXT_IDLE_DELAY_MS" src/server/state-machine.ts` shows `5_000`
- `grep -n "PERMISSION_EXEMPT_TOOLS" src/server/state-machine.ts` shows `Task.*Agent.*AskUserQuestion`
- `grep -n "AgentTimers" src/server/state-machine.ts` shows interface with permissionTimer, textIdleTimer, hadToolsInTurn
- `grep -n "turn_end" src/server/state-machine.ts` shows turn_end handling that sets waiting
- `grep -n "cancelAllTimers" src/server/state-machine.ts` shows timer cancellation on new data
- `grep -n "textIdleTimer" src/server/state-machine.ts` shows 5s text-idle timer start
- `grep -n "permissionPending: false" src/server/state-machine.ts` shows initialization in createAgent
- `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>State machine has per-agent timer infrastructure. turn_end immediately sets waiting and cancels all timers. Text-idle 5s timer starts on assistant_text and transitions to waiting. Permission timer starts on non-exempt tool_use (7s). All timers cancel on any new data. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` compiles without errors
2. `grep -rn "permissionPending" src/` shows field in types.ts and state-machine.ts
3. `grep -rn "turn_end\|turn_duration" src/server/` shows parser + state-machine handling
4. `grep -rn "TEXT_IDLE_DELAY_MS\|PERMISSION_TIMER_DELAY_MS" src/server/state-machine.ts` shows both timer constants
5. `grep -rn "cancelAllTimers" src/server/state-machine.ts` shows timer cancellation infrastructure
</verification>

<success_criteria>
- turn_duration JSONL records are parsed into turn_end AgentEvents
- turn_end sets agent to waiting and cancels all timers immediately
- Text-only turns start 5s idle timer that transitions to waiting
- permissionPending field exists on AgentState and propagates via WebSocket
- Per-agent timer infrastructure (permission + text-idle) is operational
- Permission timer starts on non-exempt tool_use (7s delay)
- All timers cancel when new JSONL data arrives for that agent
- Existing 30s/60s/5m fallback timeouts remain functional
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/08-state-detection/08-01-SUMMARY.md`
</output>
