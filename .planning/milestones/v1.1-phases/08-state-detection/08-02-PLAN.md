---
phase: 08-state-detection
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/server/state-machine.ts
autonomous: true
requirements: [DETECT-02, DETECT-03, DETECT-04]

must_haves:
  truths:
    - "비면제 도구 호출 후 7초간 응답이 없으면 permissionPending이 true로 설정된다"
    - "새 JSONL 데이터가 도착하면 진행 중인 permission 타이머와 text-idle 타이머가 즉시 취소된다"
    - "새 데이터 도착 시 permissionPending이 false로 리셋된다"
    - "run_in_background 도구 호출이 background-tool 이벤트로 파싱되어 추적된다"
    - "queue-operation 이벤트가 별도로 파싱되어 처리된다"
  artifacts:
    - path: "src/server/state-machine.ts"
      provides: "Background agent tracking with backgroundAgentIds Set"
      contains: "backgroundAgentIds"
  key_links:
    - from: "src/server/parser.ts"
      to: "src/server/state-machine.ts"
      via: "background-tool and queue-operation event types"
      pattern: "background-tool|queue-operation"
    - from: "src/server/state-machine.ts"
      to: "WebSocket broadcast"
      via: "agent-update with permissionPending changes"
      pattern: "permissionPending"
---

<objective>
Permission 타이머 검증, 타이머 취소 로직 강화, background agent 추적을 완성한다.

Purpose: Plan 01에서 구축한 타이머 인프라 위에 DETECT-02(permission 7s), DETECT-03(타이머 취소), DETECT-04(background agent)의 동작을 검증하고, background agent 추적 데이터 구조를 완성한다. Plan 01이 이미 permission timer start + cancelAllTimers + queue-operation/background-tool 핸들링의 기본 골격을 포함하므로, 이 Plan은 background agent 상태 추적과 E2E 통합 검증에 집중한다.
Output: background agent 추적이 완성된 state-machine.ts, 전체 통합 검증 완료
</objective>

<execution_context>
@C:/Users/ldm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ldm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-state-detection/08-01-SUMMARY.md

<interfaces>
<!-- Interfaces from Plan 01 output — executor should verify these exist before modifying -->

From src/shared/types.ts (after Plan 01):
```typescript
export interface AgentState {
  id: string;
  name: string;
  sessionId: string;
  status: AgentStatus;
  lastAction: string;
  lastUpdated: number;
  position: { x: number; y: number };
  permissionPending: boolean;
}

export interface AgentEvent {
  timestamp: number;
  sessionId: string;
  type: string;      // includes 'turn_end', 'queue-operation', 'background-tool'
  content: string;
  raw: unknown;
  toolName?: string;
}
```

From src/server/state-machine.ts (after Plan 01):
```typescript
const PERMISSION_TIMER_DELAY_MS = 7_000;
const TEXT_IDLE_DELAY_MS = 5_000;
const PERMISSION_EXEMPT_TOOLS = new Set(['Task', 'Agent', 'AskUserQuestion']);

interface AgentTimers {
  permissionTimer: ReturnType<typeof setTimeout> | null;
  textIdleTimer: ReturnType<typeof setTimeout> | null;
  hadToolsInTurn: boolean;
}

// Methods: getTimers(), cancelAllTimers(), cancelPermissionTimer(), cancelTextIdleTimer()
// processEvent() handles: turn_end, queue-operation, background-tool, tool_use permission timer, text-idle timer
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Background agent 추적 데이터 구조 + queue-operation/background-tool 상태 관리</name>
  <files>src/server/state-machine.ts</files>
  <read_first>src/server/state-machine.ts, src/shared/types.ts, src/server/parser.ts</read_first>
  <action>
**1. AgentTimers 인터페이스 확장** — background agent 추적 필드 추가:

AgentTimers 인터페이스에 backgroundAgentIds를 추가한다:
```typescript
interface AgentTimers {
  permissionTimer: ReturnType<typeof setTimeout> | null;
  textIdleTimer: ReturnType<typeof setTimeout> | null;
  hadToolsInTurn: boolean;
  backgroundAgentIds: Set<string>;  // tool IDs of active background agents
}
```

getTimers() 초기화도 업데이트:
```typescript
private getTimers(agentId: string): AgentTimers {
  let timers = this.agentTimers.get(agentId);
  if (!timers) {
    timers = {
      permissionTimer: null,
      textIdleTimer: null,
      hadToolsInTurn: false,
      backgroundAgentIds: new Set(),
    };
    this.agentTimers.set(agentId, timers);
  }
  return timers;
}
```

**2. processEvent() — background-tool 핸들러 강화:**

Plan 01의 기본 background-tool 핸들러를 확장하여 backgroundAgentIds에 추적 추가:
```typescript
// --- DETECT-04: background-tool (run_in_background) ---
if (event.type === 'background-tool') {
  // Extract tool call ID from raw for tracking
  const rawEntry = event.raw as Record<string, unknown>;
  const message = rawEntry?.message as Record<string, unknown> | undefined;
  const content = message?.content;
  if (Array.isArray(content)) {
    for (const block of content) {
      if (block?.type === 'tool_use' && block?.id) {
        timers.backgroundAgentIds.add(block.id as string);
        console.log(`[StateMachine] Background agent tracked: ${block.id} for ${agent.id}`);
      }
    }
  }
  agent.lastUpdated = event.timestamp;
  agent.lastAction = event.content;
  this.emit('agent-update', { ...agent });
  return;
}
```

**3. processEvent() — queue-operation 핸들러 강화:**

Plan 01의 기본 queue-operation 핸들러를 확장하여 background agent 완료 감지:
```typescript
// --- DETECT-04: queue-operation (background agent completion) ---
if (event.type === 'queue-operation') {
  // A queue-operation event typically signals background agent activity
  // Check if this is a completion signal and remove from tracking
  const rawEntry = event.raw as Record<string, unknown>;
  const toolId = rawEntry?.tool_use_id as string | undefined;
  if (toolId && timers.backgroundAgentIds.has(toolId)) {
    timers.backgroundAgentIds.delete(toolId);
    console.log(`[StateMachine] Background agent completed: ${toolId} for ${agent.id}`);
  }
  agent.lastUpdated = event.timestamp;
  this.emit('agent-update', { ...agent });
  return;
}
```

**4. 타이머 취소 로직이 올바르게 동작하는지 확인:**

processEvent() 상단에서 cancelAllTimers() + permissionPending=false 리셋이 Plan 01에 이미 구현되어 있는지 확인한다. 다음 패턴이 존재해야 한다:
```typescript
// --- DETECT-03: Any new data cancels active timers ---
this.cancelAllTimers(agent.id);
if (agent.permissionPending) {
  agent.permissionPending = false;
}
```

이 패턴이 없으면 추가한다. 있으면 그대로 둔다.

**5. removeAgent()에서 backgroundAgentIds 정리 확인:**

removeAgent()에서 cancelAllTimers() 호출 뒤 agentTimers.delete()가 backgroundAgentIds도 함께 정리하는지 확인한다 (Map.delete가 전체 AgentTimers 객체를 제거하므로 자동으로 정리됨).

**주의사항:**
- Plan 01이 이미 상당 부분을 구현했으므로, 기존 코드를 중복 작성하지 말 것
- 이 Task는 Plan 01 코드 위에 backgroundAgentIds 추적만 추가하는 것이 핵심
- permissionTimer 7s와 textIdleTimer 5s는 Plan 01에서 이미 구현 — 여기서는 건드리지 않음
  </action>
  <verify>
    <automated>cd D:/work/dev/github/ldmprog/iosys-pixel-lab && npx tsc --noEmit 2>&1</automated>
  </verify>
  <acceptance_criteria>
- `grep -n "backgroundAgentIds" src/server/state-machine.ts` shows Set in AgentTimers and usage in processEvent
- `grep -n "Background agent tracked" src/server/state-machine.ts` shows tracking log in background-tool handler
- `grep -n "Background agent completed" src/server/state-machine.ts` shows completion log in queue-operation handler
- `grep -n "cancelAllTimers" src/server/state-machine.ts` shows call at top of processEvent (DETECT-03)
- `grep -n "permissionPending" src/server/state-machine.ts` shows reset to false on new data
- `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Background agents are tracked via backgroundAgentIds Set per agent. background-tool events add tool IDs. queue-operation events remove completed IDs. All timers cancel on new data arrival. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: E2E 통합 검증 — 타입 빌드 + 서버 시작 + 이벤트 흐름 확인</name>
  <files>src/server/state-machine.ts</files>
  <read_first>src/server/state-machine.ts, src/server/parser.ts, src/shared/types.ts, src/server/index.ts</read_first>
  <action>
**1. TypeScript 전체 빌드 확인:**
```bash
npx tsc --noEmit
```
모든 에러가 0이어야 한다. 에러가 있으면 수정.

**2. 서버 시작 확인:**
```bash
# 서버를 잠시 시작해서 크래시 없이 부팅되는지 확인
timeout 5 npx tsx src/server/index.ts 2>&1 || true
```
`[Server] Pixel Office backend running on http://localhost:3333` 메시지가 나와야 한다.

**3. 파서 이벤트 흐름 확인 — 인라인 스크립트로 parser를 직접 테스트:**
```bash
node -e "
const { parseJSONLLine } = require('./dist/server/parser.js') || {};
// If CJS doesn't work, just verify the source code patterns
console.log('Parser verification: checking source patterns...');
"
```
빌드가 안 되면 소스 코드 패턴 grep으로 대체.

**4. 핵심 패턴 grep 검증:**

다음 패턴들이 모두 존재하는지 확인:
- `grep 'turn_end' src/server/parser.ts` — turn_duration 파싱
- `grep 'queue-operation' src/server/parser.ts` — progress 타입 파싱
- `grep 'background-tool' src/server/parser.ts` — run_in_background 감지
- `grep 'PERMISSION_TIMER_DELAY_MS.*7_000' src/server/state-machine.ts` — 7초 상수
- `grep 'TEXT_IDLE_DELAY_MS.*5_000' src/server/state-machine.ts` — 5초 상수
- `grep 'PERMISSION_EXEMPT_TOOLS' src/server/state-machine.ts` — 면제 도구 세트
- `grep 'cancelAllTimers' src/server/state-machine.ts` — 타이머 취소
- `grep 'backgroundAgentIds' src/server/state-machine.ts` — background 추적
- `grep 'permissionPending.*boolean' src/shared/types.ts` — 타입 필드
- `grep 'toolName.*string' src/shared/types.ts` — 이벤트 필드

**5. 에러 발견 시 수정:**
- TypeScript 컴파일 에러가 있으면 해당 파일을 수정
- 누락된 패턴이 있으면 해당 구현을 추가
- 서버 시작 시 런타임 에러가 있으면 수정

**주의사항:**
- 이 Task는 검증 + 수정 Task. 새 기능 추가가 아님
- Plan 01 + Task 1이 모두 정상이면 이 Task는 확인만 하고 끝
- 문제 발견 시에만 코드를 수정
  </action>
  <verify>
    <automated>cd D:/work/dev/github/ldmprog/iosys-pixel-lab && npx tsc --noEmit 2>&1 && echo "BUILD OK"</automated>
  </verify>
  <acceptance_criteria>
- `npx tsc --noEmit` exits with code 0 and prints "BUILD OK"
- `grep -c "turn_end" src/server/parser.ts` returns at least 1
- `grep -c "queue-operation" src/server/parser.ts` returns at least 1
- `grep -c "background-tool" src/server/parser.ts` returns at least 1
- `grep -c "PERMISSION_TIMER_DELAY_MS" src/server/state-machine.ts` returns at least 1
- `grep -c "TEXT_IDLE_DELAY_MS" src/server/state-machine.ts` returns at least 1
- `grep -c "backgroundAgentIds" src/server/state-machine.ts` returns at least 1
- `grep -c "permissionPending" src/shared/types.ts` returns at least 1
- Server starts without crash (3333 port message appears in output)
  </acceptance_criteria>
  <done>Full TypeScript build passes. Server boots without crash. All 5 DETECT requirements (turn_duration, permission 7s, timer cancellation, background agent, text-idle 5s) are implemented and verified via code pattern checks.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` compiles without errors
2. `grep -rn "backgroundAgentIds" src/server/state-machine.ts` shows per-agent background tracking
3. `grep -rn "cancelAllTimers" src/server/state-machine.ts` shows timer cancellation on every new event
4. `grep -rn "permissionPending" src/` shows field in types + state-machine + reset logic
5. Server starts cleanly: `timeout 5 npx tsx src/server/index.ts 2>&1 | grep "running on"`
</verification>

<success_criteria>
- Background agent tool IDs tracked in backgroundAgentIds Set per agent
- queue-operation events remove completed background agent IDs
- All timers (permission + text-idle) cancel on any new JSONL data for that agent
- permissionPending resets to false on new data
- TypeScript compiles cleanly
- Server boots without runtime errors
- All DETECT-01 through DETECT-05 patterns verifiable via grep
</success_criteria>

<output>
After completion, create `.planning/phases/08-state-detection/08-02-SUMMARY.md`
</output>
