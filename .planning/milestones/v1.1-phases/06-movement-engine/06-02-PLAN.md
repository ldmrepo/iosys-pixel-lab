---
phase: 06-movement-engine
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/engine/index.ts
  - src/engine/Character.ts
autonomous: false
requirements: [MOVE-01, MOVE-02, MOVE-07]

must_haves:
  truths:
    - "캐릭터가 출발지에서 목적지까지 4방향 BFS 경로를 따라 타일 단위로 이동한다"
    - "캐릭터가 자기 좌석(비walkable)을 목적지로 삼을 때 도달할 수 있다"
    - "FSM이 work->walk->idle 사이클을 정상 순환하며 각 상태에 맞는 애니메이션이 재생된다"
    - "walk 중 이동 방향에 따라 walk_down/walk_up/walk_left/walk_right 프레임이 선택된다"
  artifacts:
    - path: "src/engine/index.ts"
      provides: "좌석 배정 시 정확한 workSeat 좌표 + FALLBACK_MANIFEST walk 애니메이션"
      contains: "walk_down"
    - path: "src/engine/Character.ts"
      provides: "walk 애니메이션 리셋 + 방향 추적"
      contains: "currentDirection"
  key_links:
    - from: "src/engine/index.ts (addAgent)"
      to: "src/engine/PathFinder.ts"
      via: "requestPath callback using pathFinder.findPath"
      pattern: "this\\.pathFinder\\.findPath"
    - from: "src/engine/Character.ts (updateMovement)"
      to: "src/engine/CharacterBehavior.ts (onArrived)"
      via: "path completion triggers state transition"
      pattern: "behavior\\.onArrived"
    - from: "src/engine/CharacterBehavior.ts (goToWork)"
      to: "src/engine/index.ts (requestPath callback)"
      via: "requestPath callback invokes PathFinder for seat navigation"
      pattern: "requestPath.*targetX.*targetY"
---

<objective>
E2E 연동 검증 + gap 수정 -- FSM/BFS/좌석 코드가 실제로 정상 동작하는지 확인하고 발견된 문제를 수정한다

Purpose: Plan 01에서 walk 스프라이트와 방향 시스템을 추가했으므로, 이제 전체 파이프라인(FSM 전환 -> BFS 경로 -> walk 애니메이션 -> 도착 -> 상태 전환)이 정상 작동하는지 E2E로 검증하고, 좌석 배정 좌표 정합성 등 통합 시 발생하는 gap을 수정한다.

Output: 동작 검증 완료된 movement engine, E2E 검증 체크포인트 통과
</objective>

<execution_context>
@C:/Users/ldm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ldm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-movement-engine/06-CONTEXT.md
@.planning/phases/06-movement-engine/06-01-SUMMARY.md
@src/engine/index.ts
@src/engine/Character.ts
@src/engine/CharacterBehavior.ts
@src/engine/PathFinder.ts
@src/shared/types.ts
@src/shared/asset-manifest.ts
@src/shared/office-layout.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix integration gaps -- workSeat coordinates, animation reset on walk start, FALLBACK_MANIFEST walk entries</name>
  <files>src/engine/index.ts, src/engine/Character.ts</files>

  <read_first>
    - src/engine/index.ts (addAgent method lines 220-270, FALLBACK_MANIFEST, cacheMovementTiles)
    - src/engine/Character.ts (updateMovement, setPath, getEffectiveStatus, update method)
    - src/engine/CharacterBehavior.ts (goToWork, goToBreak, goToWander, onArrived)
    - src/shared/office-layout.ts (seats array with tileX/tileY coordinates)
    - src/shared/types.ts (Seat interface, AgentStatus type)
  </read_first>

  <action>
**1. Fix workSeat coordinate in addAgent() (index.ts)**

Current code at line 249 sets workSeat from `state.position` which is the initial spawn position, not necessarily the actual seat tile. This should use the assigned seat from the layout:

```typescript
// In addAgent(), find the character's assigned seat from layout
const assignedSeat = this.layout.seats.find(s => !s.assignedAgentId || s.assignedAgentId === state.id);
const workSeat = assignedSeat
  ? { x: assignedSeat.tileX, y: assignedSeat.tileY }
  : { x: state.position.x, y: state.position.y };

// Mark seat as assigned
if (assignedSeat && !assignedSeat.assignedAgentId) {
  assignedSeat.assignedAgentId = state.id;
}
```

Update the setBehavior call to use this computed `workSeat` instead of `{ x: state.position.x, y: state.position.y }`.

Also update `removeAgent()` to release the seat:
```typescript
removeAgent(id: string): void {
  this.characters.delete(id);
  // Release seat assignment
  const seat = this.layout.seats.find(s => s.assignedAgentId === id);
  if (seat) {
    seat.assignedAgentId = undefined;
  }
}
```

**2. Add FALLBACK_MANIFEST walk animation entries (index.ts)**

Update the FALLBACK_MANIFEST.characters.default.animations to include the 4 walk directions:
```typescript
walk_down:  { frames: [28, 29, 30, 31], fps: 8, loop: true },
walk_up:    { frames: [32, 33, 34, 35], fps: 8, loop: true },
walk_right: { frames: [36, 37, 38, 39], fps: 8, loop: true },
walk_left:  { frames: [40, 41, 42, 43], fps: 8, loop: true },
```

**3. Reset animation timer when walk starts (Character.ts)**

In `setPath()`, reset animation timing so walk animation starts from frame 0:
```typescript
setPath(path: PathNode[]): void {
  if (path.length === 0) return;
  this.path = path;
  this.pathIndex = 0;
  // Reset animation for clean walk start
  this.animTime = 0;
  this.currentFrameIndex = 0;
}
```

**4. Verify PathFinder allows non-walkable destination for seat tiles**

Read PathFinder.ts line 96-98 to confirm this logic exists:
```typescript
if (!this.isWalkableFn(nx, ny) && !(nx === endX && ny === endY)) {
  continue;
}
```

This already handles MOVE-07 (seat is non-walkable but allowed as destination). No change needed -- just verify it's present and intact.

**5. Verify cacheMovementTiles() applies furniture walkableMask**

Confirm cacheMovementTiles in index.ts applies furniture footprints to walkGrid. This was implemented in v1.0. Verify the walkGrid is passed to `tileMap.setWalkGrid(walkGrid)` and that `pathFinder` uses `tileMap.isWalkable`. No change needed if intact.

**6. Ensure getEffectiveStatus handles all behavior states correctly**

Verify Character.ts `getEffectiveStatus()` covers:
- `behavior.isWalking` -> returns `walk_down`/`walk_up`/`walk_left`/`walk_right` (done in Plan 01)
- `behavior.shouldPlayWorkAnim` -> returns `this.state.status` (typing/reading/executing)
- `behavior.state === 'idle'` -> returns `'idle'`
- `behavior.state === 'work' && !shouldPlayWorkAnim` -> returns `'idle'` (resting at desk)

If any of these are not covered by Plan 01 changes, add them.
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && echo "---TSC---" && npx vite build 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - src/engine/index.ts addAgent method uses layout.seats to find assigned seat (contains `this.layout.seats.find`)
    - src/engine/index.ts addAgent method contains `assignedSeat.assignedAgentId = state.id`
    - src/engine/index.ts removeAgent method contains `assignedAgentId = undefined` (seat release)
    - src/engine/index.ts FALLBACK_MANIFEST contains `walk_down:` and `walk_up:` and `walk_right:` and `walk_left:`
    - src/engine/Character.ts setPath method contains `this.animTime = 0` (animation reset)
    - src/engine/PathFinder.ts line 96-98 contains the non-walkable destination exception (unchanged)
    - `npx tsc --noEmit` exits 0
    - `npx vite build` succeeds
  </acceptance_criteria>

  <done>좌석 배정이 layout.seats에서 올바르게 할당되고, walk 시작 시 애니메이션이 리셋되며, FALLBACK_MANIFEST에 walk 애니메이션이 포함되고, 빌드가 정상 통과한다</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: E2E 동작 검증 -- FSM/BFS/walk 애니메이션 통합 테스트</name>
  <files>none (verification only)</files>

  <action>
Start the dev server with `npm run dev`, then present the following verification scenarios to the user. The executor should confirm the dev server starts without errors before handing off to the user.
  </action>

  <what-built>
    Phase 6 Movement Engine 전체:
    1. 4방향 walk 스프라이트시트 (128x352, 11행)
    2. Character.ts 방향 추적 + walk_down/up/left/right 애니메이션 선택
    3. CharacterBehavior FSM (work/walk/idle 전환)
    4. PathFinder BFS 경로탐색 (4방향, 비walkable 좌석 도달 가능)
    5. 좌석 배정 + 해제 로직
  </what-built>

  <how-to-verify>
    1. `npm run dev` 실행하여 개발 서버 시작
    2. 브라우저에서 http://localhost:5173 접속
    3. 아래 시나리오 확인:

    **시나리오 A: 기본 렌더링**
    - 캐릭터가 오피스에 표시되는지 확인
    - 캐릭터가 정지 상태에서 idle 애니메이션(미세 움직임)을 재생하는지 확인

    **시나리오 B: 자율 이동 (FSM idle -> walk 전환)**
    - 30초 정도 관찰하여 캐릭터가 자동으로 이동을 시작하는지 확인
    - 이동 시 캐릭터가 타일을 따라 걷는지 (대각선 없이 상하좌우로)
    - 이동 방향에 따라 캐릭터 스프라이트가 변경되는지 (위로 갈 때 뒷모습, 좌우로 갈 때 옆모습)
    - 이동 중 walk 애니메이션(4프레임 걷기)이 재생되는지

    **시나리오 C: 좌석 복귀**
    - 배회 후 캐릭터가 자기 좌석으로 돌아가는지 확인
    - 좌석(비walkable 타일)에 정상 도달하는지

    **시나리오 D: 장애물 회피**
    - 캐릭터가 가구를 뚫지 않고 우회하여 이동하는지

    문제가 있으면 구체적으로 어떤 시나리오가 실패하는지 설명해주세요.
  </how-to-verify>

  <verify>
    <automated>npm run dev 2>&1 | head -10</automated>
  </verify>

  <done>사용자가 4개 시나리오(기본 렌더링, 자율 이동, 좌석 복귀, 장애물 회피) 모두 정상 동작을 확인하고 "approved" 입력</done>

  <resume-signal>Type "approved" if movement works correctly, or describe specific issues</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. `npx vite build` succeeds
3. E2E: 캐릭터가 idle/walk 상태를 전환하며 이동
4. E2E: 이동 방향에 따라 4방향 walk 스프라이트가 재생
5. E2E: 캐릭터가 좌석(비walkable)으로 정상 도달
6. E2E: 캐릭터가 장애물을 우회하여 이동
</verification>

<success_criteria>
- FSM work->walk->idle 사이클이 정상 순환
- BFS 경로를 따라 4방향 타일 단위 이동
- 이동 방향에 맞는 walk_down/walk_up/walk_left/walk_right 애니메이션 재생
- 자기 좌석(비walkable) 도달 가능
- 빌드 에러 없음
- 사용자 E2E 검증 통과
</success_criteria>

<output>
After completion, create `.planning/phases/06-movement-engine/06-02-SUMMARY.md`
</output>
