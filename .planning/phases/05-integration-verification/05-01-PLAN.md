---
phase: 05-integration-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/office-layout.ts
  - src/engine/index.ts
  - src/server/state-machine.ts
  - src/server/index.ts
autonomous: false
requirements: [R5.4]

must_haves:
  truths:
    - "createPod generates exactly 4 seats per pod, total 20 seats across 5 pods"
    - "StateMachine loads real office-layout seats BEFORE session discovery starts"
    - "FALLBACK_LAYOUT uses 30x24 dimensions matching real office layout"
    - "npm run typecheck passes with zero errors"
    - "Agents are visually assigned to work zone seats and walk/animate correctly"
    - "All furniture objects have walkableMask applied (R5.2)"
  artifacts:
    - path: "src/shared/office-layout.ts"
      provides: "Fixed createPod producing 4 seats per pod (20 total)"
      contains: "Total seat count: .* = 20 seats"
    - path: "src/server/state-machine.ts"
      provides: "Awaitable layout loading via layoutReady promise"
      contains: "waitForLayout"
    - path: "src/server/index.ts"
      provides: "Server startup awaits layout before discovery"
      contains: "waitForLayout"
    - path: "src/engine/index.ts"
      provides: "FALLBACK_LAYOUT with 30x24 dimensions"
      contains: "width: 30"
  key_links:
    - from: "src/server/index.ts"
      to: "src/server/state-machine.ts"
      via: "await stateMachine.waitForLayout() before discovery.start()"
      pattern: "await.*waitForLayout.*discovery\\.start"
    - from: "src/shared/office-layout.ts"
      to: "src/server/state-machine.ts"
      via: "dynamic import loads 20 real seats"
      pattern: "Loaded.*seats"
    - from: "src/engine/index.ts"
      to: "src/shared/office-layout.ts"
      via: "dynamic import loads layout, FALLBACK_LAYOUT matches dimensions"
      pattern: "width: 30.*height: 24"
---

<objective>
Fix three defects blocking Phase 5 acceptance: (1) createPod generates 8 seats per pod instead of 4 (40 total vs 20-24 target), (2) StateMachine async race condition where agents get DEFAULT_SEATS before real layout loads, (3) FALLBACK_LAYOUT still uses old 20x15 dimensions. Then visually verify the complete integrated system.

Purpose: This is the FINAL phase of the v1.0 milestone. All prior phases (types, layout, tilemap, renderer) are complete. These three defects are the only blockers before the expanded 30x24 office with 6 zones, 20 seats, and 80+ furniture objects is fully operational.

Output: Working integrated system with correct seat assignment, no async race, and visual confirmation.
</objective>

<execution_context>
@C:/Users/ldm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ldm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-integration-verification/05-RESEARCH.md
@.planning/phases/04-objectrenderer-sprites/04-01-SUMMARY.md
@.planning/phases/02-office-layout-design/02-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/shared/types.ts:
```typescript
export interface Seat {
  id: string;
  tileX: number;
  tileY: number;
  deskTileX: number;
  deskTileY: number;
  facing: 'up' | 'down' | 'left' | 'right';
  assignedAgentId?: string;
}

export interface OfficeLayout {
  width: number;
  height: number;
  tileSize: number;
  tiles: TileInfo[][];
  furniture: FurnitureObject[];
  seats: Seat[];
}
```

From src/server/state-machine.ts (current structure to modify):
```typescript
export class AgentStateMachine extends EventEmitter {
  private seats: Seat[] = [...DEFAULT_SEATS];
  constructor() {
    super();
    this.loadOfficeLayout(); // fire-and-forget — THE BUG
  }
  private async loadOfficeLayout(): Promise<void> { ... }
  start(): void { ... }
}
```

From src/server/index.ts (current startup to modify):
```typescript
server.listen(PORT, () => {
  discovery.start();      // may fire before layout loads — THE BUG
  stateMachine.start();
});
```

From src/shared/office-layout.ts createPod (current buggy version):
```typescript
function createPod(sx: number, sy: number, prefix: string) {
  for (let i = 0; i < 2; i++) {   // TWO columns — produces 8 seats, not 4
    const dx = sx + i * 3;
    // top desk: 2 chairs
    // bottom desk: 2 chairs
    // = 4 per column x 2 columns = 8 per pod
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix seat count bug and update FALLBACK_LAYOUT</name>
  <files>src/shared/office-layout.ts, src/engine/index.ts</files>

  <read_first>
    - src/shared/office-layout.ts (lines 285-355 — createPod function and pod calls)
    - src/engine/index.ts (lines 68-85 — FALLBACK_SEATS and FALLBACK_LAYOUT)
  </read_first>

  <acceptance_criteria>
    - grep "for (let i = 0; i < 2" src/shared/office-layout.ts returns ZERO matches (outer 2-column loop removed)
    - grep -c "seats.push" src/shared/office-layout.ts returns exactly 10 (2 source lines per createPod call-site after outer loop removal, invoked across 5 pods at runtime yielding 20 seats)
    - grep "Total seat count" src/shared/office-layout.ts shows "= 20 seats"
    - grep "width: 30" src/engine/index.ts matches in FALLBACK_LAYOUT
    - grep "height: 24" src/engine/index.ts matches in FALLBACK_LAYOUT
    - npm run typecheck exits 0
  </acceptance_criteria>

  <action>
  **Part A — Fix createPod to produce 4 seats per pod (not 8):**

  In `src/shared/office-layout.ts`, rewrite the `createPod` function (around line 288) to use a SINGLE column instead of two. Remove the outer `for (let i = 0; i < 2; i++)` loop entirely. Use `sx` directly as the desk x-position (no `dx = sx + i * 3`).

  The fixed function should produce exactly 4 seats per pod:
  - Top desk at (sx, sy): 2 chairs at (sx, sy+1) and (sx+1, sy+1) facing down
  - Bottom desk at (sx, sy+3): 2 chairs at (sx, sy+2) and (sx+1, sy+2) facing up
  - Pod footprint becomes 2 tiles wide x 5 tiles tall (was 6 wide)

  Keep the same furniture push patterns (desk, monitor, chairs) but only for ONE column. Preserve renderWidth, renderHeight, drawOffsetY values from Phase 4.

  Update the function JSDoc comment to say: "Pod footprint: 2 tiles wide x 5 tiles tall. Produces 4 seats per pod."

  Then update the pod call comments (lines ~343-352):
  - Zone A line: "3 pods, 12 seats" stays correct (3 pods x 4 = 12)
  - Zone B line: "2 pods, 8 seats" stays correct (2 pods x 4 = 8)
  - Total line: "Total seat count: 12 (Zone A) + 8 (Zone B) = 20 seats" (this was already written but was wrong; now it will be correct)

  Adjust the pod call x-positions to space them properly in the narrower footprint:
  - A1: createPod(10, 2, 'A1') — x:10-11, no change needed
  - A2: createPod(15, 2, 'A2') — moved from x:17 to x:15 (tighter spacing, still within workA x=9..28)
  - A3: createPod(20, 2, 'A3') — moved from x:23 to x:20 (still within workA)
  - B1: createPod(3, 11, 'B1') — moved from x:2 to x:3 (center better in workB x=1..14)
  - B2: createPod(9, 11, 'B2') — x:9-10, no change needed

  NOTE: Do NOT touch any furniture objects outside of createPod. Do NOT change server racks, meeting room, lounge, lobby, or wall decoration objects. Per research Pitfall 4, modifying unrelated furniture risks breaking sortY/rendering.

  **Part B — Update FALLBACK_LAYOUT in engine/index.ts:**

  In `src/engine/index.ts`, update line ~76:
  - `generateFallbackTiles(20, 15)` -> `generateFallbackTiles(30, 24)`
  - FALLBACK_LAYOUT: `width: 20` -> `width: 30`, `height: 15` -> `height: 24`

  Leave FALLBACK_SEATS as-is (5 seats is fine for a fallback that should never activate).
  </action>

  <verify>
    <automated>cd D:/work/dev/github/ldmprog/iosys-pixel-lab && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>

  <done>createPod produces exactly 4 seats per pod. 5 pods yield 20 total seats (s1-s20). FALLBACK_LAYOUT uses 30x24. TypeCheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Fix StateMachine async race condition</name>
  <files>src/server/state-machine.ts, src/server/index.ts</files>

  <read_first>
    - src/server/state-machine.ts (full file — constructor, loadOfficeLayout, start methods)
    - src/server/index.ts (lines 115-125 — server.listen callback)
  </read_first>

  <acceptance_criteria>
    - grep "layoutReady" src/server/state-machine.ts returns at least 2 matches (field + assignment)
    - grep "waitForLayout" src/server/state-machine.ts returns at least 1 match (method definition)
    - grep "waitForLayout" src/server/index.ts returns at least 1 match (called before discovery)
    - grep "await.*waitForLayout" src/server/index.ts returns 1 match
    - grep "async.*=>" src/server/index.ts confirms the listen callback is async
    - npm run typecheck exits 0
  </acceptance_criteria>

  <action>
  **Part A — Add awaitable layout loading to AgentStateMachine:**

  In `src/server/state-machine.ts`, modify the class:

  1. Add a private field: `private layoutReady: Promise<void>;`
  2. In the constructor, change `this.loadOfficeLayout();` to `this.layoutReady = this.loadOfficeLayout();`
  3. Add a public method:
     ```typescript
     async waitForLayout(): Promise<void> {
       await this.layoutReady;
     }
     ```

  This stores the promise from the existing `loadOfficeLayout()` call so it can be awaited externally. The `loadOfficeLayout` method itself does NOT need changes — it already returns `Promise<void>` and handles errors gracefully.

  **Part B — Make server startup await layout before discovery:**

  In `src/server/index.ts`, change the `server.listen` callback from synchronous to async, and await the layout load before starting discovery:

  Change:
  ```typescript
  server.listen(PORT, () => {
    // ... console.log lines ...
    discovery.start();
    stateMachine.start();
  });
  ```

  To:
  ```typescript
  server.listen(PORT, async () => {
    // ... console.log lines (keep as-is) ...

    // Ensure office layout seats are loaded before discovering sessions
    await stateMachine.waitForLayout();

    discovery.start();
    stateMachine.start();
  });
  ```

  This ensures that when `discovery.start()` scans for existing sessions and calls `onSessionAdded`, the real 20 seats from office-layout.ts are already loaded — not the 12 DEFAULT_SEATS.
  </action>

  <verify>
    <automated>cd D:/work/dev/github/ldmprog/iosys-pixel-lab && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>

  <done>StateMachine exposes waitForLayout(). Server startup awaits layout loading before starting session discovery. No agent can receive DEFAULT_SEATS positions during normal operation. TypeCheck passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual integration verification</name>
  <files>none (verification only)</files>

  <action>
  Run `npm run dev` and visually verify the complete integrated system. This is the final gate for the v1.0 milestone.

  **What was built in Tasks 1-2:**
  1. Seat count reduced from 40 to 20 (4 per pod instead of 8)
  2. Async race condition eliminated (layout loads before session discovery)
  3. FALLBACK_LAYOUT updated to 30x24

  **Verification steps:**
  1. Run `npm run dev` in the project root
  2. Open http://localhost:5173 in a browser
  3. Verify the 30x24 office renders with 6 distinct zone colors (dark navy server room, brown work zones, green meeting room, beige lounge, cream lobby, light beige corridors)
  4. Verify furniture is visible: server racks (top-left), desk pods in work zones, meeting room table, lounge sofas, lobby reception
  5. Check the terminal for server log: `[StateMachine] Loaded 20 seats from office-layout` — must show exactly 20 seats
  6. Check the terminal for engine log: `[Engine] Cached N walkable tiles, M break tiles` — N should be > 200, M > 50
  7. If any Claude Code sessions are active, verify agents appear at work zone desks and animate (typing/idle/walking)
  8. Pan and zoom the canvas to verify camera works across the full 30x24 map
  9. **walkableMask verification (R5.2):** Confirm that no runtime error is thrown by the walkableMask assertion in office-layout.ts (lines 628-631). The existing guard `const missing = furniture.filter(f => !f.walkableMask); if (missing.length > 0) throw ...` runs at module load time. If the server starts without crashing, all furniture has walkableMask. Additionally, run: `grep -c "walkableMask" src/shared/office-layout.ts` and confirm the count is >= 60 (every furniture.push call includes walkableMask). Verify visually that agents pathfind around furniture (not through it), confirming walkableMask is applied correctly in the engine.
  </action>

  <verify>
    <automated>cd D:/work/dev/github/ldmprog/iosys-pixel-lab && npm run typecheck 2>&1 | tail -3 && node -e "const m = require('child_process').execSync('grep -c walkableMask src/shared/office-layout.ts').toString().trim(); console.log('walkableMask occurrences:', m); if (parseInt(m) < 60) { console.error('FAIL: expected >= 60 walkableMask entries'); process.exit(1); } else { console.log('PASS: all furniture has walkableMask'); }"</automated>
  </verify>

  <done>Human approves visual rendering. Server log confirms "Loaded 20 seats". Engine log confirms walkable/break tile caching. All furniture has walkableMask applied (R5.2 — verified by runtime assertion at module load and grep count >= 60). v1.0 milestone complete.</done>
</task>

</tasks>

<verification>
- `npm run typecheck` exits with 0 errors
- Server logs show `Loaded 20 seats from office-layout`
- Engine logs show `Cached N walkable tiles, M break tiles` with N > 200
- createPod function has no outer 2-column loop
- FALLBACK_LAYOUT dimensions are 30x24
- server.listen callback awaits waitForLayout before discovery.start
- All furniture has walkableMask applied (runtime assertion passes, grep count >= 60)
- Human approves visual rendering of the complete integrated system
</verification>

<success_criteria>
- 20 seats total (AC: 20~24 range satisfied)
- No async race condition (layout loads before discovery)
- npm run typecheck: 0 errors
- Visual verification: 6 zones render, furniture visible, agents at desks
- WebSocket pipeline intact (agents respond to session events)
- All furniture objects have walkableMask (R5.2 satisfied)
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-verification/05-01-SUMMARY.md`
</output>
