---
phase: 06-movement-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/composite-characters.ts
  - public/assets/sprites/claude.png
  - public/assets/sprites/codex.png
  - public/assets/sprites/gemini.png
  - src/shared/types.ts
  - src/shared/asset-manifest.ts
  - src/engine/SpriteSheet.ts
  - src/engine/Character.ts
autonomous: true
requirements: [MOVE-01]

must_haves:
  truths:
    - "각 캐릭터 스프라이트시트에 4방향 x 4프레임 walk 애니메이션이 포함된다"
    - "캐릭터가 이동할 때 이동 방향(상하좌우)에 맞는 walk 프레임이 선택된다"
    - "캐릭터가 idle/walk/type 세 상태 사이를 전환하며 상태마다 올바른 애니메이션이 재생된다"
  artifacts:
    - path: "scripts/composite-characters.ts"
      provides: "4방향 walk 행이 추가된 스프라이트시트 생성 스크립트"
      contains: "OUT_ROWS = 11"
    - path: "public/assets/sprites/claude.png"
      provides: "확장된 128x352 스프라이트시트"
    - path: "src/engine/SpriteSheet.ts"
      provides: "수평 반전 drawFrame 지원"
      contains: "drawFrameFlipped"
    - path: "src/engine/Character.ts"
      provides: "방향 추적 + walk 상태 애니메이션 선택 로직"
      contains: "currentDirection"
  key_links:
    - from: "src/engine/Character.ts"
      to: "src/engine/SpriteSheet.ts"
      via: "drawFrameFlipped for LEFT direction"
      pattern: "drawFrameFlipped"
    - from: "src/engine/Character.ts"
      to: "src/shared/asset-manifest.ts"
      via: "walk animation frame indices"
      pattern: "walk_down.*walk_up.*walk_right"
---

<objective>
4방향 walk 스프라이트시트 생성 + 엔진 방향 시스템 구현

Purpose: 캐릭터가 상하좌우 이동 시 해당 방향에 맞는 walk 애니메이션을 재생하려면, 스프라이트시트에 4방향 walk 프레임이 있어야 하고 엔진이 이동 방향을 추적하여 올바른 프레임을 선택해야 한다.

Output: 확장된 스프라이트시트(128x352), 방향 추적 + walk 프레임 선택 로직이 통합된 Character.ts
</objective>

<execution_context>
@C:/Users/ldm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ldm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-movement-engine/06-CONTEXT.md
@src/shared/types.ts
@src/shared/asset-manifest.ts
@src/engine/SpriteSheet.ts
@src/engine/Character.ts
@scripts/composite-characters.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend composite-characters.ts to generate 4-direction walk rows + regenerate sprites</name>
  <files>scripts/composite-characters.ts, public/assets/sprites/claude.png, public/assets/sprites/codex.png, public/assets/sprites/gemini.png, src/shared/types.ts, src/shared/asset-manifest.ts</files>

  <read_first>
    - scripts/composite-characters.ts (current 7-row output layout and MetroCity column mapping)
    - src/shared/types.ts (AgentStatus type, SpriteAnimation, CharacterSprite interfaces)
    - src/shared/asset-manifest.ts (standardAnimations, characterSprite function)
  </read_first>

  <action>
**1. Extend composite-characters.ts output layout from 7 rows to 11 rows (128x352)**

Current layout uses only down-direction frames. Add 4 new walk rows using all MetroCity directions.

MetroCity column layout (24 cols per row):
- Down:  cols 0-5  (idle1, idle2, walk1, walk2, walk3, walk4)
- Left:  cols 6-11 (idle1, idle2, walk1, walk2, walk3, walk4)
- Right: cols 12-17 (idle1, idle2, walk1, walk2, walk3, walk4)
- Up:    cols 18-23 (idle1, idle2, walk1, walk2, walk3, walk4)

New output layout (4 cols x 11 rows, 128x352):
```
Row  0: idle      [down_idle1, down_idle2, down_idle1, down_idle2]  (unchanged)
Row  1: typing    [down_idle1, down_idle2, down_idle1, down_idle2]  (unchanged)
Row  2: reading   [down_idle1, down_idle2, -, -]                    (unchanged)
Row  3: executing [down_walk1, down_walk2, down_walk3, down_walk4]  (unchanged)
Row  4: waiting   [down_idle1, down_idle2, -, -]                    (unchanged)
Row  5: done      [down_idle1, down_idle2, -, -]                    (unchanged)
Row  6: error     [down_idle1, down_idle2, -, -]                    (unchanged)
Row  7: walk_down [down_walk1, down_walk2, down_walk3, down_walk4]  (NEW)
Row  8: walk_up   [up_walk1,   up_walk2,   up_walk3,   up_walk4]    (NEW)
Row  9: walk_right[right_walk1,right_walk2,right_walk3,right_walk4] (NEW)
Row 10: walk_left [left_walk1, left_walk2, left_walk3, left_walk4]  (NEW)
```

Update constants:
```typescript
const OUT_ROWS = 11;
const OUT_H = FRAME * OUT_ROWS; // 352
```

Add new row mappings to OUTPUT_ROWS array. The MetroCity walk columns per direction are:
- Down walk:  srcCols [2, 3, 4, 5] with direction offset 0
- Up walk:    srcCols [20, 21, 22, 23] (offset 18)
- Right walk: srcCols [14, 15, 16, 17] (offset 12)
- Left walk:  srcCols [8, 9, 10, 11] (offset 6)

Add these 4 entries to OUTPUT_ROWS:
```typescript
// Row 7: walk_down [walk1..walk4 from down direction cols 2-5]
{ frames: [{ outCol: 0, srcCol: 2 }, { outCol: 1, srcCol: 3 }, { outCol: 2, srcCol: 4 }, { outCol: 3, srcCol: 5 }] },
// Row 8: walk_up [walk1..walk4 from up direction cols 20-23]
{ frames: [{ outCol: 0, srcCol: 20 }, { outCol: 1, srcCol: 21 }, { outCol: 2, srcCol: 22 }, { outCol: 3, srcCol: 23 }] },
// Row 9: walk_right [walk1..walk4 from right direction cols 14-17]
{ frames: [{ outCol: 0, srcCol: 14 }, { outCol: 1, srcCol: 15 }, { outCol: 2, srcCol: 16 }, { outCol: 3, srcCol: 17 }] },
// Row 10: walk_left [walk1..walk4 from left direction cols 8-11]
{ frames: [{ outCol: 0, srcCol: 8 }, { outCol: 1, srcCol: 9 }, { outCol: 2, srcCol: 10 }, { outCol: 3, srcCol: 11 }] },
```

Note: Since MetroCity provides actual left-direction sprites, we use them directly rather than horizontal-flipping right sprites. This avoids needing flip logic in the composite script.

**2. Run the updated script to regenerate all 3 sprite sheets**

```bash
npx tsx scripts/composite-characters.ts
```

This produces:
- `public/assets/sprites/claude.png` (128x352)
- `public/assets/sprites/codex.png` (128x352)
- `public/assets/sprites/gemini.png` (128x352)

**3. Add `walk_down`, `walk_up`, `walk_right`, `walk_left` to AgentStatus type in types.ts**

Update the AgentStatus union:
```typescript
export type AgentStatus = 'idle' | 'typing' | 'reading' | 'executing' | 'waiting' | 'done' | 'error'
  | 'walk_down' | 'walk_up' | 'walk_right' | 'walk_left';
```

**4. Update asset-manifest.ts standardAnimations to include walk directions**

Add walk animation entries using the new rows (frame index = row * 4 + col):
```typescript
const standardAnimations: Record<AgentStatus, SpriteAnimation> = {
  idle:       anim([0, 1], 2, true),
  typing:     anim([4, 5, 6, 7], 6, true),
  reading:    anim([8, 9], 3, true),
  executing:  anim([12, 13, 14, 15], 8, true),
  waiting:    anim([16, 17], 2, true),
  done:       anim([20, 21], 2, false),
  error:      anim([24, 25], 4, true),
  walk_down:  anim([28, 29, 30, 31], 8, true),  // Row 7
  walk_up:    anim([32, 33, 34, 35], 8, true),  // Row 8
  walk_right: anim([36, 37, 38, 39], 8, true),  // Row 9
  walk_left:  anim([40, 41, 42, 43], 8, true),  // Row 10
};
```

Walk fps = 8 (same as executing, gives brisk walk feel at 3 tiles/sec).

**5. Update DEFAULT_ANIMATIONS in Character.ts to match**

Add the same 4 walk entries to the DEFAULT_ANIMATIONS constant at the top of Character.ts:
```typescript
const DEFAULT_ANIMATIONS: Record<AgentStatus, SpriteAnimation> = {
  idle:       { frames: [0, 1],          fps: 2,  loop: true },
  typing:     { frames: [4, 5, 6, 7],    fps: 8,  loop: true },
  reading:    { frames: [8, 9],          fps: 3,  loop: true },
  executing:  { frames: [12, 13, 14, 15], fps: 6, loop: true },
  waiting:    { frames: [16, 17],        fps: 2,  loop: true },
  done:       { frames: [20, 21],        fps: 1,  loop: true },
  error:      { frames: [24, 25],        fps: 4,  loop: true },
  walk_down:  { frames: [28, 29, 30, 31], fps: 8, loop: true },
  walk_up:    { frames: [32, 33, 34, 35], fps: 8, loop: true },
  walk_right: { frames: [36, 37, 38, 39], fps: 8, loop: true },
  walk_left:  { frames: [40, 41, 42, 43], fps: 8, loop: true },
};
```

**6. Update FALLBACK_MANIFEST in engine/index.ts to include walk animations**

Add the same 4 walk entries to the animations object in FALLBACK_MANIFEST.characters.default.
  </action>

  <verify>
    <automated>npx tsx scripts/composite-characters.ts 2>&1 | grep -q "128x352" && echo "SPRITES OK" || echo "SPRITES FAIL"</automated>
  </verify>

  <acceptance_criteria>
    - scripts/composite-characters.ts contains `OUT_ROWS = 11`
    - scripts/composite-characters.ts contains `OUT_H = FRAME * OUT_ROWS`
    - scripts/composite-characters.ts OUTPUT_ROWS array has 11 entries (rows 0-10)
    - public/assets/sprites/claude.png exists and is 128x352
    - public/assets/sprites/codex.png exists and is 128x352
    - public/assets/sprites/gemini.png exists and is 128x352
    - src/shared/types.ts contains `walk_down` and `walk_up` and `walk_right` and `walk_left` in AgentStatus
    - src/shared/asset-manifest.ts contains `walk_down:` and `walk_up:` and `walk_right:` and `walk_left:`
    - src/engine/Character.ts DEFAULT_ANIMATIONS contains `walk_down` and `walk_up` and `walk_right` and `walk_left`
    - `npx tsc --noEmit` exits 0 (no type errors)
  </acceptance_criteria>

  <done>3개 캐릭터 스프라이트시트가 128x352 (11행)로 확장되고, AgentStatus 타입에 walk_down/walk_up/walk_right/walk_left가 추가되며, asset-manifest와 DEFAULT_ANIMATIONS에 walk 애니메이션 정의가 포함된다</done>
</task>

<task type="auto">
  <name>Task 2: Add direction tracking to Character.ts + update getEffectiveStatus for walk animation</name>
  <files>src/engine/Character.ts, src/engine/SpriteSheet.ts</files>

  <read_first>
    - src/engine/Character.ts (getEffectiveStatus method, updateMovement method, render method)
    - src/engine/SpriteSheet.ts (drawFrame method, current API)
    - src/engine/CharacterBehavior.ts (isWalking, shouldPlayWorkAnim properties)
    - src/shared/types.ts (AgentStatus type including new walk_* variants)
  </read_first>

  <action>
**1. Add direction tracking to Character.ts**

Add a `currentDirection` field to the Character class:
```typescript
/** Current facing direction, updated during path movement. */
private currentDirection: 'down' | 'up' | 'left' | 'right' = 'down';
```

**2. Update `updateMovement()` to track direction**

In the section where the character moves toward a path waypoint (around line 217-236), compute and store direction from the movement delta:

After computing `dx` and `dy` (before the movement step), add:
```typescript
// Update facing direction based on movement
if (Math.abs(dx) > Math.abs(dy)) {
  this.currentDirection = dx > 0 ? 'right' : 'left';
} else if (dy !== 0) {
  this.currentDirection = dy > 0 ? 'down' : 'up';
}
```

This goes right after the `const dy = targetY - this.worldY;` line and before `const dist = ...`.

**3. Update `getEffectiveStatus()` to return direction-specific walk status**

Replace the current walking branch that returns `'idle'`:

BEFORE:
```typescript
if (this.behavior.isWalking) {
  return 'idle'; // Walking uses idle animation frames
}
```

AFTER:
```typescript
if (this.behavior.isWalking) {
  // Return direction-specific walk animation
  switch (this.currentDirection) {
    case 'up':    return 'walk_up';
    case 'down':  return 'walk_down';
    case 'left':  return 'walk_left';
    case 'right': return 'walk_right';
  }
}
```

**4. Handle non-behavior walking case**

Also check `this.isMoving` (path active) for characters without behavior FSM. In `getEffectiveStatus()`, add before the final `return this.state.status`:

```typescript
// Non-behavior characters: use walk animation when moving along path
if (this.isMoving) {
  switch (this.currentDirection) {
    case 'up':    return 'walk_up';
    case 'down':  return 'walk_down';
    case 'left':  return 'walk_left';
    case 'right': return 'walk_right';
  }
}
```

**5. No changes needed to SpriteSheet.ts**

The existing `drawFrame` method handles the new frame indices (28-43) without modification, since it calculates `row = floor(frameIndex / columns)` and `col = frameIndex % columns`. With a 4-column sheet, row 7-10 frames map correctly.

Note: The LEFT direction has dedicated sprites from MetroCity, so we do NOT need horizontal flip. The `drawFrameFlipped` method in must_haves was precautionary -- since MetroCity provides left-direction frames natively, skip implementing it. Update must_haves note: SpriteSheet does not need modification.
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - src/engine/Character.ts contains `private currentDirection: 'down' | 'up' | 'left' | 'right' = 'down'`
    - src/engine/Character.ts updateMovement method contains `this.currentDirection = dx > 0 ? 'right' : 'left'`
    - src/engine/Character.ts getEffectiveStatus method contains `return 'walk_down'` (not `return 'idle'` for walking)
    - src/engine/Character.ts getEffectiveStatus method contains `return 'walk_up'`
    - src/engine/Character.ts getEffectiveStatus does NOT contain the old `return 'idle'; // Walking uses idle animation frames` line
    - `npx tsc --noEmit` exits 0 (all types satisfy the updated AgentStatus union)
  </acceptance_criteria>

  <done>Character.ts tracks movement direction and returns walk_down/walk_up/walk_left/walk_right from getEffectiveStatus() when walking, so the correct directional walk animation plays during movement</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npx tsx scripts/composite-characters.ts` regenerates 128x352 sprites successfully
3. AgentStatus type includes walk_down, walk_up, walk_right, walk_left
4. Character.ts getEffectiveStatus returns direction-specific walk status (not 'idle') during movement
5. DEFAULT_ANIMATIONS and standardAnimations both include all 4 walk direction entries
</verification>

<success_criteria>
- 스프라이트시트가 11행(128x352)으로 확장되어 4방향 walk 프레임 포함
- 캐릭터가 이동 시 방향에 맞는 walk_down/walk_up/walk_left/walk_right 애니메이션 재생
- TypeScript 컴파일 에러 없음
- 기존 idle/typing/reading/executing/waiting/done/error 애니메이션 불변
</success_criteria>

<output>
After completion, create `.planning/phases/06-movement-engine/06-01-SUMMARY.md`
</output>
